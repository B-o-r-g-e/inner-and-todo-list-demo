import {
    addID,
    addTasks,
    addTodoToLibrary,
    articles,
    calendar,
    createTodo,
    openCalendar,
    taskCountFunc
} from "./page-load.js";

addTodoToLibrary('Go home', 'At this point you’ve already had a fair amount of practice ')
articles()
createTodo()
addTasks()
addID()
taskCountFunc()
calendar()
openCalendar()